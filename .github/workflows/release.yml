name: Release

# 推送形如 v1.0.1 的 tag 时触发自动打包并发布到 GitHub Releases
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write   # 创建 Release、上传附件需要

jobs:
  build:
    runs-on: windows-latest   # WinForms + WebView2 + Velopack 需在 Windows 上打包

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - name: Derive version from tag
        id: ver
        shell: pwsh
        run: |
          # refs/tags/v1.0.1 -> 1.0.1
          $tag = "${{ github.ref_name }}"
          $version = $tag.TrimStart('v')
          "version=$version" >> $env:GITHUB_OUTPUT
          "tag=$tag" >> $env:GITHUB_OUTPUT

      - name: Install Velopack CLI
        run: dotnet tool install -g vpk

      - name: Publish (self-contained)
        env:
          CI: "true"   # 跳过 csproj 里的本地 PostPublish 打包
        run: >
          dotnet publish MYUBrowser/MYUBrowser.csproj
          -c Release -r win-x64 --self-contained true
          -o publish

      - name: Velopack pack
        run: >
          vpk pack
          --packId MYUBrowser
          --packVersion ${{ steps.ver.outputs.version }}
          --packDir publish
          --mainExe MYUBrowser.exe
          --packTitle "MYU Browser"
          --outputDir releases

      - name: Upload to GitHub Releases
        run: >
          vpk upload github
          --repoUrl https://github.com/${{ github.repository }}
          --publish
          --releaseName "MYU Browser ${{ steps.ver.outputs.version }}"
          --tag ${{ steps.ver.outputs.tag }}
          --outputDir releases
          --token ${{ secrets.GITHUB_TOKEN }}
